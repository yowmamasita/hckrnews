[project]
name = "hn"
version = "0.1.0"
description = "Command line tool to display HN top stories"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.3",
    "rich>=14.0.0",
    "textual>=3.2.0",
]

[project.scripts]
hn = "hn.app:main"
